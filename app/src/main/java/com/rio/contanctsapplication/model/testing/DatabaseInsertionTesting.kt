package com.rio.contanctsapplication.model.testing

import android.location.Address
import android.location.Geocoder
import java.util.*

class DatabaseInsertionTesting {
//    val geocoder = Geocoder(, Locale.getDefault())
//
//    val addresses: List<Address> = geocoder.getFromLocation(
//        12.801,
//        80.12344,
//        1
//    ) // Here 1 represent max location result to returned, by documents it recommended 1 to 5
//
//
//    val address: String =
//        addresses[0].getAddressLine(0) // If any additional address line present than only, check with max available address lines by getMaxAddressLineIndex()
//
//    val city: String = addresses[0].locality
//    val state: String = addresses[0].adminArea
//    val country: String = addresses[0].countryName
//    val postalCode: String = addresses[0].postalCode
//
//    Toast.makeText(this, addresses.toString(), Toast.LENGTH_SHORT).show()
//    Log.i("city", city)
//    Log.i("city", state)
//    Log.i("city", country)
//    Log.i("city", postalCode)
//    Log.i("city", addresses.toString())
}